// This module has been deprecated and removed from the active application flow.
// It is preserved here only to prevent build errors if referenced elsewhere implicitly.
import React from 'react';
import { TimeModuleProps } from '../types';

const SiderealModule: React.FC<TimeModuleProps> = () => {
  return null;
};

export default SiderealModule;